import PlaidLinkFlow from "./components/PlaidLinkFlow";

const App = () => {
  return (
    <main className="flex flex-col items-center justify-center bg-bg min-h-screen">
      <PlaidLinkFlow />
    </main>
  );
};

export default App;
